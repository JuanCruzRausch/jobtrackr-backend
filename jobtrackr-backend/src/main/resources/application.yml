spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jobtrackr
    username: jobadmin
    password: 123456
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update  # "update" crea/actualiza tablas seg�n tus entidades
    show-sql: true      # para ver las consultas SQL en consola
    properties:
      hibernate:
        format_sql: true
  jackson:
    serialization:
      INDENT_OUTPUT: true

jwt:
  # 64 bytes en Base64 para HS512 (solo ejemplo; cambia en prod)
  secret: "dWJha2JZcXZ3eWVydGlvU2VjdXJlS2V5QmFzZTY0NjRCeXRlc01pbmRZb3VyT3duIQ=="
  expirationMs: 2592000000  # 30 dias

server:
  port: 8080